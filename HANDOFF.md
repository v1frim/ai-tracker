# Handoff — AI Progress Tracker

> Контекст для нового чату (після `/clear`). Оновлено: **2026-07-15**.
> Історія конкретних змін — у `git log`; тут лише актуальний стан, механіки й підводні камені.
> Тримай цей файл коротким: нові записи — стисло, застаріле — видаляй.

## Де ми зараз

- Гілка розробки: **`claude/add-youtube-channel-5u3f7b`** (стоїть на `trusting-brown-m5wcks`; push до `main`/`claude/**` → авто-деплой на Pages, `.github/workflows/deploy.yml`).
- ⚠️ **`main` сильно позаду розробки** — нову гілку створювати ЛИШЕ від актуальної dev-гілки, інакше пуш відкотить живий сайт (сталося 11.07: відкат встиг помилково видати проєктні ачівки, виправлено міграцією `projAchFix_v2`).
- Весь застосунок — один файл **`src/AITracker.jsx`** (~5000 рядків). Константи — **`src/constants.js`** (SKILLS, ачівки, `ACTIVITY_DEFS`). Інструменти описані в `TOOLS.md`.
- **Завжди `npm run build` перед пушем.**
- Останнє зроблене (сесія 11–15.07): міграція `projAchFix_v2` (−1300 XP хибних ачівок відкату); SYNTX.AI у `YT_CHANNELS` з захардкодженим channelId (хардкод тепер перебиває кеш); категорія «🎛️ Агрегатори» (Syntx/Higgsfield/Runway) + merge skillData з дефолтами; 12-та активність «📝 Пости» (`content_posts_published`, 15 XP); Reve і Eyecandy в інструментах; збереження стану зливає `...saved` (незнайомі поля переживають старі збірки — фікс втрати inbox).
- **Зроблено повний аудит XP** (усі джерела/ставки/пули: ачівки 248 650, навички 207 300, дохід 3 XP/$, сесії 0 XP; рівень 100 = рівно 1 000 000 XP). Інтерактивний XP-планер (артефакт): https://claude.ai/code/artifact/33ba7461-fb77-4390-bf88-a640cc13d2de — вихідник у **`docs/xp-planner.html`** (щоб оновити з нової сесії: редагуй файл і передай Artifact-у цей url; Вовині правки живуть у localStorage сторінки, ключ `xp_planner_v2` — структуру стейту не ламати, інакше скинуться). Вовині фінальні цифри доходу/сесій — ТІЛЬКИ в його браузері: перед перенесенням у код попросити скрін/цифри.
- **XP-ребаланс ВНЕСЕНО в код (15.07, фінальні цифри Вови):** дохід — спадна шкала `INCOME_XP_TIERS` за кумулятивною сумою: $0–1k → 5 XP/$, до $20k → 3, до $300k → 1, далі 0 (маржинально через `incomeXPUpTo`, constants.js); сесії — день N стріку дає `SESSION_XP_BASE=10` + `SESSION_XP_STEP=2` × ⌊N/10⌋ (авто-сесія в AITracker, source `"session"`); фікс ×2 у «Фокусі» (fToggleDone тепер дає/знімає дедлайн-бонус симетрично); правила прописані в UI вкладок Сесії та Фінанси (генеруються з констант). Статистика: derivedSession тепер з xpLog, а не dates×5. Ставки активностей НЕ мінялися (рішення Вови: XP-зміни лише дохід+сесії).

## Вкладки (10)

🏠 Головна · 🎯 Цілі & проєкти · 🛠️ Інструменти · 💪 Навички · 🏆 Досягнення · 💸 Фінанси · 🔥 Сесії · 📝 Нотатки · 🎵 Радіо · 📊 Статистика — масив `tabs` ~ряд. 1885.

## «🎯 Цілі & проєкти» (`activeTab === "goalsplan"`, IIFE) — ядро

4-рівнева ієрархія (кожен рівень може містити нижчі):

| Рівень  | Масив state | Колір    | defXP | Поле-зв'язок дитини |
|---------|-------------|----------|-------|---------------------|
| 🎯 Ціль   | `longGoals` | #c084fc | 1000 | `goalId`    |
| 🚀 Проєкт | `projects`  | #fbbf24 | 200  | `projectId` |
| 📋 План   | `plan`      | #22d3ee | 50   | `planId`    |
| ✅ Задача | `goals` ⚠️  | #00ff88 | 10   | — (лист)    |

⚠️ **Підступно:** `goals` = ЗАДАЧІ, а цілі — `longGoals`.

Хелпери IIFE: `TYPE_META`, `LINK_FIELD`, `CAN_CONTAIN`; `dropToType` (drag між секціями = зміна типу, XP скидається); `reparentChildren`; `canNestInto`/`nestUnder` (drag на рядок = вкладення; чистять усі три поля-зв'язки — батько завжди один); `setPinnedCascade` (📌 каскадно → «Фокус» на Головній); інлайн-редагування назви/XP (`gpTextEdit`/`gpXpEdit`); soft-delete (`deletedAt`, макс 10 видалених); 💡 Інбокс ідей (`inbox`, конвертація `doConvertInboxItem`, drag-reorder).

## Модель даних (localStorage `ai_tracker_v1`)

- Збереження в useEffect — шукай `const state = {`.
- Ієрархія: `longGoals` / `projects` / `plan` / `goals`(задачі) / `inbox`. Спільні поля: `{ id, text, done, createdAt, pinned?, deletedAt?, completedAt?, xpAwarded?, deadline?, deadlineBonus? }` + одне поле-зв'язок + специфічні (`customXP` у цілі/проєкту, `xp` у плану/задачі, `category`+`status` у проєкту, `type`+`urgency` у плану, `period` у цілі).
- Активності: `metricLog` (`{date,key,delta}`, ключі = `ACTIVITY_DEFS.key`) — джерело правди для статистики й відновлення `todayActivity` на старті. Лічильники — `learnTime` / `skillTasksData`.
- Дні: `totalActiveDays = sessions.dates.length` — єдине джерело для всього, що рахує активні дні. `daysSinceStart` — календарні від `APP_START_DATE=2026-06-01` (окрема метрика, законно більша).

## Ключові механіки

- XP: рівень = `Math.sqrt(xp/80)`. Реконсиляція на старті (`computeCorrectActivityXP`) ретроактивно перецінює лічильники при зміні ставок в `ACTIVITY_DEFS`.
- Активності на Головній: 12 карток (3×4), XP-сходинка image 1 < аудіо 6 < кліпи 8 < пости 15 < музика 30 < відео 40 < клієнти 100 < автоматизація 300 < сайти 500. Пости ділять лічильник `content_posts_published` з навичкою (майлстоуни поверх +15/шт).
- **Дедлайни цілей/проєктів:** поле `deadline` (ISO), чип 📅 на рядку прогрес-бару (`renderDeadline`), ×2 XP при вчасному виконанні (`deadlineMet` у `makeToggleDone`, прапорець `deadlineBonus`; подвоюється лише власний XP, `commitXpEdit` теж враховує).
- Бліда плашка «+N XP» (`xpCell`) біля квестів-контейнерів — рекурсивна сума XP підзадач; суто інформаційна.
- Підписки (💸 Фінанси): щомісячне нагадування `subPrompt` з редагованими сумами (для UAH за курсом); список показує «наступне списання» (повторення `billingDay` після `lastBilledYM`; призупинені — «останнє {дата}»).
- Статистика: зведена помісячна таблиця (рядок «До» = нерозподілені залишки до 1 червня, Σ = `metricTotal`); картки `MetricPeriods` теж мають період «До» (проп `total`).
- GitHub-синхронізація «Рядків коду» (`githubSync`, вкладка 📊) — жива фіча з власним token (AI-чат-бот видалено, це не він).

## Підводні камені

- `activeDays` + `recordActiveDay` (≈7 викликів) — вестигіальні: пишуться, ніде не показуються. Можна вичистити окремою чисткою.
- Вкладка 🛠 називає лічильники «Відео/Музика», а активності — «Кліпи/Аудіо» (ключі `video_videos_created`/`music_tracks_created` спільні). Свідома косметична розбіжність.
- Одноразові міграції даних — узор біля `projAchFix` (~ряд. 1527), приклад: `ai_tracker_beforeMove_v1`.
- Stop-hook «Unverified» — хибна тривога (нема GPG в середовищі), ігнорувати.
- Початкові дані не скидати: XP=300 (Oxford_1000), перший проєкт Oxford, ачівка `oxford_dev`.

## Ідеї на потім

- Дублювати у «Фокусі» на Головній: дедлайн-чип і плашку «+N XP».
- Ачівки за виконані-вчасно; нагадування про близькі дедлайни.
- Ачівки-етапи для `full_videos`/`full_music`/`sites_built`.
- Перейменувати лічильники у 🛠 (Відео→Кліпи, Музика→Аудіо).
- Злити «Днів з ШІ»+«Активних» у формат `18/22`.
- Таблиця Статистики: заморозити крайні колонки при скролі; майбутні порожні місяці.
- Рідкісність цілей (бронза/срібло/золото за XP-вагою); сезонний прогрес-бар.
