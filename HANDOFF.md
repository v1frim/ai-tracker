# Handoff — AI Progress Tracker

> Контекст для продовження в новому чаті (після `/clear`). Останнє оновлення: **2026-06-25**.

## Що зроблено в ЦІЙ сесії (2026-06-25)

- **Бліда «+N»-плашка із сумарним XP підзадач** у вкладці «🎯 Цілі & проєкти». Поряд із власним (редагованим) XP кожного **квесту-контейнера** (ціль/проєкт/план) тепер показується бліде `+N` — рекурсивна сума XP усіх вкладених підзадач (без видалених). Мета: бачити повну вагу квесту, не лише власний XP. На реальний облік XP **не впливає** (суто інформаційне).
  - Код в IIFE «Цілі & проєкти» (`src/AITracker.jsx`): хелпери `xpVal` / `xpChildren` / `subtaskXP` (рекурсія) + плашка `xpCell` — одразу після `xpBadge` (~ряд. 3249). У рядках замінено `xpBadge(...)` → `xpCell(...)` для плану/проєкта/цілі; **задача** (листя) лишилась на `xpBadge`.
  - Безпечно від подвоєнь: поля `goalId`/`projectId`/`planId` взаємовиключні (`nestUnder`/`reparentChildren` чистять усі три перед призначенням одного) → кожен елемент має одного батька.
  - Тултип блідої плашки: «Разом X XP — власний +A та +B з підзадач». Показується лише коли сума підзадач > 0.
  - Можливі дороблення (не зроблено): дублювати таку ж плашку у «Фокусі» на Головній; за бажанням показувати **сумарний підсумок** замість «додаткового +N» (зараз свідомо «+N з підзадач» — як просив Вова).

- **Видалено AI-чат-бот** (FAB-кнопка 🤖 у правому нижньому куті) та все повʼязане. Вова ним не користувався, а цей чат ефективніший. Прибрано: стан віджета (`aiOpen`/`aiMessages`/`aiInput`/`aiModel`/`aiApiKeys` тощо), збереження `aiMessages`/`aiModel`/`aiApiKeys` у localStorage (тепер зникають при наступному збереженні), ефект автоскролу й сам віджет (~294 рядки, бандл −16 КБ).
  - ⚠️ **GitHub-синхронізація** («Рядків коду» у Статистиці: `githubSync`/`ghPanelOpen`/`ghSyncing`/`ghSyncMsg` + `syncGithubLines`, UI у вкладці 📊) — **окрема фіча, лишилась**. Використовує власний `githubSync.token`, не ключі бота.

- **Бліда плашка підзадач → «+N XP»** (раніше «+N»). Додано одиниці в `xpCell` для ясності.

- **Ширший селект категорій проєкту** (`maxWidth` 54→124) — довгі назви («Робота / Клієнти») більше не обрізаються.

- **Дедлайни для цілей і проєктів (×2 XP за вчасне виконання).** Поле `deadline` (ISO `YYYY-MM-DD`) на елементах `longGoals`/`projects`.
  - UI на **другому рядку** (де прогрес-бар): чип `📅 DD.MM · ще N дн ⚡×2` (`renderDeadline`, ~після `xpCell`). Клік по чипу → інлайн `<input type="date">` (стан `gpDeadlineEdit`); × у чипі прибирає дедлайн. Коли дедлайну ще нема — ледь помітна 📅 серед дій (✏️/📌) додає його. Колір чипа за терміном: прострочено — червоний (без ⚡×2), ≤3 дн — бурштин, ≤7 — колір типу, далі — зелений; виконано вчасно — зелений «вчасно ⚡×2», інакше «після терміну».
  - **×2 логіка** в `makeToggleDone`: `deadlineMet(x)` = `today ≤ deadline`. При вчасному завершенні `gainXP(base*2)` + ставиться прапорець `deadlineBonus:true`; відкат знімає `base*2`. **Лише власний XP** цілі/проєкту (підзадачі не подвоюються). `commitXpEdit` теж множить різницю на 2, якщо `deadlineBonus`.
  - Перевірено e2e (Playwright): вчасно→+200 (2×100, мітка «⚡×2»), прострочено/без-дедлайну→+100, відкат→−200; прапорець `deadlineBonus` зберігається й коректно відкочується.
  - Можливі дороблення: дублювати дедлайн-чип у «Фокусі» на Головній; досягнення за виконані-вчасно; нагадування про близькі дедлайни.

- **🐞 Фікс розбіжності днів.** Стат «Активних» брав `activeDays.length`, а досягнення/стрік/хітмапа — `sessions.dates`. `activeDays` поповнювався лише на частині дій (`recordActiveDay`), тому відставав (показував 23 проти 24). Тепер `totalActiveDays = sessions.dates.length` (рядок ~1180) — єдине джерело правди для всього, що рахує «дні з ШІ-активністю». «Днів з ШІ» (`daysSinceStart`, календарні від `APP_START_DATE`, +1 включно) лишається окремою метрикою — це загальний календарний проміжок, законно більший за активні дні (різниця = пропущені дні).
  - ⚠️ Масив `activeDays` + `recordActiveDay` (≈7 викликів) тепер **вестигіальні** (пишуться, але ніде не показуються). Можна прибрати в окремій чистці (стан 844, нормалізація 1138, обʼєкт стану 1009/deps 1012, визначення 1547, виклики, deps-масиви). Лишив свідомо, щоб не чіпати багато місць заради косметики.

- **Зведена помісячна таблиця в Статистиці** (між «Джерела XP» і «Фінанси»). Один рядок = місяць (від найранішого датованого запису до поточного), колонки: № · усі 11 активностей (`ACTIVITY_DEFS`, emoji-заголовки з `title`) · ⏱ години навчання (`(education+business)*0.5`) · 💰 чистий дохід (`дохід − витрати`) · Місяць · Рік. Поточний місяць підсвічено зеленим, рік показано раз на групу (на рядку зміни року), внизу рядок **Σ Разом**.
  - Дані: активності агрегуються з `metricLog` (`{date,key,delta}`, ключі = `ACTIVITY_DEFS.key`); дохід/витрати — з `incomeEntries`/`expenseEntries` через `toUSD`. Активність — лише з моменту появи обліку (як і решта розбивок); дохід — за всі датовані записи (тому місяці до старту, напр. квітень-витрати, теж показуються).
  - Код: інлайн-IIFE у вкладці stats (`activeTab === "stats"`), одразу після блоку «Джерела XP». Самодостатній (`agg`, `months`, рендер `<table>`), нічого нового в state.
  - Можливі дороблення: заморозити перші/останні колонки при горизонт. скролі; додати майбутні порожні місяці для планування; перемкнути дохід на цілі $ замість центів.
  - **Доопрацьовано:** (1) темний фон контейнера (gradient як `.wf-card`); (2) місяці тепер строго від 1 червня (`APP_START_DATE`), а все до того + **нерозподілені залишки** (загальний лічильник `metricTotal` − датоване червнем+, напр. 170−70=100 зображень) зведено в окремий рядок **«До»** на початку; Σ Разом = `metricTotal`/`net` (тобто До+місяці); (3) числа в комірках **центровано** під emoji-заголовками (було праворуч), емодзі 14px; (4) прибрано `cursor:help` (показував «?»), лишився `title` із назвою активності.

## Де ми зараз

- Гілка розробки: **`claude/trusting-brown-m5wcks`** (пуш сюди → авто-деплой на GitHub Pages).
- Живий сайт: https://v1frim.github.io/ai-tracker/ · Репо: https://github.com/v1frim/ai-tracker
- Весь застосунок — один файл: **`src/AITracker.jsx`** (~5000 рядків). Дані-константи — **`src/constants.js`** (SKILLS, ачівки, дефолти).
- Збірка: `npm run build`. **Завжди перевіряй білд перед пушем.**
- Деплой: push до `main` або `claude/**` → GitHub Actions (`.github/workflows/deploy.yml`).

## Вкладки (10)

🏠 Головна · 🎯 Цілі & проєкти · 🛠️ Інструменти · 💪 Навички · 🏆 Досягнення · 💸 Фінанси · 🔥 Сесії · 📝 Нотатки · 🎵 Радіо · 📊 Статистика

(масив `tabs` ~рядок 1885; стара окрема вкладка «Проєкти» влита в «Цілі & проєкти».)

## Вкладка «🎯 Цілі & проєкти» (`activeTab === "goalsplan"`, рендериться як IIFE)

Це ядро застосунку. **4-рівнева ієрархія** (кожен рівень може мати дітей нижчих рівнів):

| Рівень  | Масив state | Колір    | defXP | Поле зв'язку з дитиною |
|---------|-------------|----------|-------|------------------------|
| 🎯 Ціль   | `longGoals` | #c084fc | 1000 | `goalId`    |
| 🚀 Проєкт | `projects`  | #fbbf24 | 200  | `projectId` |
| 📋 План   | `plan`      | #22d3ee | 50   | `planId`   |
| ✅ Задача | `goals` ⚠️  | #00ff88 | 10   | — (лист)   |

⚠️ **Важливо/підступно:** масив `goals` тримає **ЗАДАЧІ**, а не цілі. Цілі — це `longGoals`.

Ключові хелпери всередині IIFE:
- `TYPE_META` — мапа тип → {setter, arr, prefix, color, defXP, label}.
- `LINK_FIELD` = {goal:"goalId", project:"projectId", plan:"planId", task:null}; `CAN_CONTAIN` — що який тип може містити.
- `dropToType` — drag між секціями = зміна типу (новий id, XP скидається на дефолт типу).
- `reparentChildren` — при зміні типу переносить дітей на нового батька (щоб не відривались).
- `canNestInto` / `nestUnder` — drag елемента **на рядок-батька** = вкладення (рядок підсвічується).
- `setPinnedCascade` — 📌 каскадно пінить дітей; запінене показується в «Фокусі» на Головній.
- **Інлайн-редагування рядка:** ✏️ → назва (`nameCell` + `commitTextEdit`, стан `gpTextEdit`); клік по XP-плашці → XP (`xpBadge` + `commitXpEdit`, стан `gpXpEdit`); у проєкта ще категорія/статус через `<select>`.
- Soft-delete (`deletedAt`) + секції «✓ Виконано» та «✗ Видалені» (тримає макс 10 видалених).
- **💡 Інбокс ідей** — `inbox` state, масове вставлення; конвертація картки в ціль/проєкт/план/задачу з вибором батька (`doConvertInboxItem`, `gpInboxConvert`); інлайн-редагування назви (✏️ → `gpInboxEdit` / `commitInboxEdit`); drag-reorder зі смужкою-вставкою (`InboxGapZone`, дзеркало `GapZone`) + зони зміни типу (`ibzone_`).

## Що зроблено в попередній сесії (2026-06-22)

1. **Активності 6 → 9** (`constants.js` → `ACTIVITY_DEFS`). Сітка карток на Головній стала рівно **3×3** з логічним групуванням по рядках:
   - Рядок 1 (час): 📚 Навчання · 💼 Бізнес · 📺 Навч.відео — без змін.
   - Рядок 2 (**сирі уривки**): 🎨 Зображення · 🎞 **Кліпи** (було «Відео», ключ `video_videos_created`) · 🎧 **Аудіо** (було «Музика», ключ `music_tracks_created`). Змінено лише `label`/`emoji`/`color` — ключі, лічильники, XP ті самі (перейменування безпечне).
   - Рядок 3 (**готові твори, нові `learn`-лічильники в `learnTime`**): 🎬 **Відео** (`full_videos`, +40) · 🎵 **Музика** (`full_music`, +30) · 🌐 **Сайти/додатки** (`sites_built`, +500 — бо сайт постійно доробляється).
   - Ідея: і відео, і звук діляться на «уривок → готове» (відео = монтаж кліпів; музика = твір зі звукових уривків). Сайти = код+ідея+реалізація, тому найбільший XP.
   - XP-сходинка: image 1 < аудіо 6 < кліпи 8 < музика 30 < відео 40 < сайти 500 (легко крутити в `ACTIVITY_DEFS`).
   - ⚠️ Вкладка 🛠 Інструменти/навички досі показує ті самі лічильники як «Відео/Музика» (бо `video_videos_created`/`music_tracks_created`). Косметична розбіжність із новими назвами «Кліпи/Аудіо» — лишено свідомо, не критично.
   - Вова має 3 готові сайти (цей трекер, англ-сайт, когнітивка) — їх можна залогувати разово: у картці «Сайти» вписати `3` у поле N → `+N` (дасть +150 XP).
2. **Стрік під стиль гри** (вкладка 🔥 Сесії). Рядок із 4 карток + бар «Ціль місяця» замінено одним **горизонтальним ігровим баром** (натхнення — рейтинговий бар із гри): сегменти 🔥 **Стрік** · 📆 **Днів з ШІ** (календарні) · 📈 **Активних** · 🏅 **Рекорд**. Колір/сяйво сегмента «Стрік» — за tier-ом стріку (`TIERS`, ті самі пороги 3/7/30/90/180/365). IIFE одразу перед хітмапою (~ряд. 2699).
3. **Прибрано «серію за місяць»:** картку «Цього місяця», бар «Ціль місяця» (Сесії) і стат «Сесій/міс» у шапці. Шапковий стат → «**Днів з ШІ**» (`daysSinceStart`, календарні від `APP_START_DATE=2026-06-01`). Видалено осиротілі `monthSessions`/`daysPassedThisMonth`/`daysInCurrentMonth` (лишок `updateMonthlyTarget`/`monthlyTarget` — мертвий ще до цієї сесії, не чіпав).
4. **Перерахунок XP активностей** (`ACTIVITY_DEFS`): зображення 2→1, відео 20→40, музика 15→30, **сайти 50→500**. ⚠️ Реконсиляція на старті (`computeCorrectActivityXP`) ретроактивно перецінює наявні лічильники (напр. 168 зображень×1 замість ×2 → totalXP мінус ~168).
5. **Шапку статів перероблено в ігровий бар** (стиль «Сесій»): єдиний горизонтальний контейнер замість окремих боксів, іконка→число→підпис, роздільники, золоте сяйво. 7 сегментів: 💰 Дохід · 🚀 Проєкти · 🤝 Клієнти · 🏆 Досягнення · 📆 Днів з ШІ · 📈 Активних · 🔥 Стрік. Стат-бокс «Активних» додано; стрік-група в порядку Днів·Активних·Стрік; колір стріку — за `streak` (раніше за `totalActiveDays`). Аватар+ім'я лишились окремим рядком над баром.
6. **Статистика → Джерела XP:** `auto-fill`→`auto-fit` (картки заповнюють ширину, числа влазять), flex-колонка + `marginTop:auto` (рядок «Сьогодні/Загалом» на однаковій висоті), `nowrap` числам.
7. **🐞 Фікс «XP за сьогодні зникає після перезавантаження».** Крос-чек в ініціалізації `todayActivity` (рядок ~882) порівнював лічильники×**нову**_ставку з XP, залогованим за **старою** ставкою → при зміні ставок не сходилось і блок скидався в `{}`. Тепер `todayActivity` відновлюється з **`metricLog`** (записи `{date,key,delta}`, не залежить від ставок). `metricLog` цілий → фікс ретроактивний. `ai_tracker_today_act` більше не читається на старті (запис лишив — нешкідливий, у BACKUP_KEYS).
8. **Статистика перебудована.** Прибрано застарілий `SKILL_STAT_ROWS` (LLM Промптів/Задачі, Голос, Дизайн, старий Контент/Монетизація). Замість секції «Навички — виконано» — нові згруповані лічильники з Сьог/Міс/Рік (`MetricPeriods` з `metricLog`): **🎨 Контент** (Зображення/Кліпи/Відео), **🔊 Звук** (Аудіо/Музика), **🛠️ Розробка** (Сайти/Автоматизації/Рядків коду), **🎯 Квести — виконано** (Цілі/Проєкти/Плани/Задачі — розбивка по `completedAt`). Хелпери в IIFE Статистики: `metricTotal(key)` (learnTime→skillTasksData), `mkMetric`, `mkQuest`, масиви `CAT_SECTIONS`/`QUEST_SECTION`. «Час на вивчення» лишилось (вже було в Статистиці, ніде не дублюється).
9. **Нові активності на Головній: 🤝 Клієнти (+100), 🤖 Автоматизація (+300).** `skill`-kind, спільні лічильники `monetize_clients` / `automation_automations_created` (ті, що в шапці/статистиці). Сітка стала 11 карток (3×3 + 2).
10. **Нові досягнення** (`constants.js`): група **clients** (1·5·25·100·250·1000) і **automation** (1·3·5·10·25·50) + у `ACH_GROUPS`. Проведено клієнтів/автоматизації як 9-й/10-й аргумент у `check`/`progress`: оновлено `checkAchievements` (рахує з `skillTasksRef`), `achArgs` (ряд. ~3010) і додано окремий `useEffect` на `clientsCnt`/`autoCnt` (дзеркало проєктного).

### Ідеї/хвости від цієї сесії (на потім)
- Сітка Головної — 11 карток (останній ряд 2). За бажанням додати 12-ту (напр. 🎙 Озвучка / 🎨 Дизайн) для рівного 3×4.
- Чи перейменувати лічильники й у вкладці 🛠 (Відео→Кліпи, Музика→Аудіо), щоб не було розбіжності з активностями.
- «Днів з ШІ» зараз окремим сегментом від «Активних»; за бажанням можна злити в один формат `активні/календарні` (напр. `18/22`).
- Нові «готові» категорії (`full_videos`/`full_music`/`sites_built`) — прості лічильники без ачівок-етапів. За бажанням підвʼязати до власних досягнень.

## Модель даних (localStorage, ключ `ai_tracker_v1`)

Зберігається в useEffect (шукай `const state = {`). Багато ключів (фінанси, радіо, сесії тощо). Для ієрархії:
- `longGoals`, `projects`, `plan`, `goals`(=задачі), `inbox`.
- Спільні поля елемента: `{ id, text, done, createdAt, pinned?, deletedAt?, completedAt?, xpAwarded? }` + одне поле-зв'язок (`goalId`/`projectId`/`planId`) + специфічні (`customXP` для цілі/проєкту, `xp` для плану/задачі, `category`+`status` у проєкту, `type`+`urgency` у плану, `period` у цілі).

## Інструменти (вкладка 🛠️) — `SKILLS` у `constants.js`

Кожна категорія: `{ id, name, emoji, color, tools: [{ name, url, active }] }`. `active:true` = кольоровий чип («використовую»), `false` = сірий («можна спробувати»). `TOTAL_TOOLS` рахується автоматично з SKILLS. Деталі по інструментах — у `TOOLS.md`.

## Ідеї на потім (обговорені, не зроблені)

- Дедлайни на елементах + секція «На сьогодні».
- Рідкісність цілей (бронза/срібло/золото за XP-вагою + сяйво).
- Сезонний прогрес-бар (квартал/місяць).

## Примітки

- Stop-hook іноді пише «Unverified» — хибна тривога (немає GPG-підпису в середовищі). Ігнорувати.
- Початкові дані не скидати: XP=300 (Oxford_1000), перший проєкт Oxford, ачівка `oxford_dev`.
